data:
  raw_dir: "../data/raw"
  enriched_dir: "../data/enriched"

spark:
  app_name: "sparkle-movie"
  driver_memory: "6g"
  shuffle_partitions: 8

als:
  rank: 10
  max_iter: 10
  reg_param: 0.1
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15

api:
  host: "0.0.0.0"
  port: 8000
  n_recommendations: 10
